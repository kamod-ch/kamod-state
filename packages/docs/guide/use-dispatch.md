# Use dispatch

See the API reference and quick start for the MVP usage of `@kamod-ch/state`.

- Context carries the store instance only.
- Use `useSelector` for selected state subscriptions.
- Use TanStack Query for server-state caching and Formisch for form state.
