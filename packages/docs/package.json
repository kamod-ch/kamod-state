{
  "name": "@kamod-ch/state-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "prepublishOnly": "node -e \"process.exit(1)\"",
    "brand:sync": "kamod-brand-sync-docs --target public",
    "predev": "pnpm run brand:sync",
    "prebuild": "pnpm run brand:sync",
    "dev": "preactpress dev",
    "check": "preactpress check",
    "build": "preactpress build",
    "preview": "preactpress preview"
  },
  "dependencies": {
    "@kamod-ch/state": "workspace:*"
  },
  "devDependencies": {
    "@kamod-ch/brand": "^0.1.0",
    "@kamod-ch/preactpress": "^2.0.0",
    "preact": "^10.26.0",
    "typescript": "^5.7.0"
  }
}
